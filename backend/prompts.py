# prompts.py
# The scheduling system now uses a 2-step pipeline (see agent.py + intent.py).
# The LLM only performs intent extraction via a minimal prompt in intent.py.
# This file is kept as a placeholder in case a full NL prompt is ever needed.
